/// Class representing an weapon damage
class DiceSet {
  /// Number of dices
  int quantity;
  /// Sides of the dice i.e. 6 if D6, 10 if D10, etc.
  int sides;

  DiceSet(this.quantity, this.sides);
}